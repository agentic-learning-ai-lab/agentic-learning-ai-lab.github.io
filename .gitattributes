# Git LFS removed 2026-05-19 as part of the CF migration. Binary assets
# now live on Cloudflare R2 (mirrored on cdn.agenticlearning.ai) and are
# referenced via the cdnUrl/pictureCdn Handlebars helpers + the manifest
# at assets-manifest.json. Local working tree still has the binaries on
# disk (gitignored); `npm run pull:r2` rehydrates a fresh clone.
#
# See notes/lfs-migration.md for the full design and recovery path.
#
# LaTeX source still lives as tar.gz on R2 — research/<slug>/latex/ never
# enters git. See notes/latex-tarball-storage.md.
